# Task Tracker CLI (Spring Boot + JSON Storage)

A simple command-line Task Tracker built with **Java 17**, **Spring Boot**, and a JSON file for storage.  
This CLI allows you to add, update, delete, and list tasks directly from the terminal — no database needed.

---

## 🔗 Project URL (required for submission)

**GitHub Repository:**  
👉 https://github.com/abdallaheeid/task-tracker

---

## 🚀 Features

- Add new tasks
- Update task descriptions
- Delete existing tasks
- Mark tasks as **todo**, **in-progress**, or **done**
- List tasks (all or filtered by status)
- Tasks are stored in a local `tasks.json` file
- Tested with JUnit, Mockito, AssertJ

---

## 🛠 Build the Project

Using Maven Wrapper:

```bash
   ./mvnw clean package
